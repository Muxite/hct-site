{
  "name": "hct-render",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side renderer: turns data/publications.yaml into the HCT site Publications section.",
  "scripts": {
    "test": "node --test"
  }
}
